{
  "name": "@8688bnb/monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "turbo": "^2.1.2"
  },
  "packageManager": "npm@10.9.3",
  "dependencies": {
    "bcryptjs": "^3.0.3"
  }
}
